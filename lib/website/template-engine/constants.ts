/** Semantic version of the Website Builder Template Engine runtime. */
export const WB_TEMPLATE_ENGINE_VERSION = "1.0.0";

/** Subdirectory under `templates/` where installable website templates live. */
export const WB_TEMPLATES_DIR_SEGMENT = "website";

/** Relative path from project root to the website templates directory. */
export const WB_TEMPLATES_RELATIVE_PATH = `templates/${WB_TEMPLATES_DIR_SEGMENT}`;
