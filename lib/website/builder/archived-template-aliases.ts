import type { WebsiteStructureTemplate } from "@/lib/website/builder/structure-templates";

/** Legacy marketplace ids that resolve to active flagship packages (no on-disk scaffold tree). */
export function buildArchivedAliasStructureTemplates(): WebsiteStructureTemplate[] {
  return [];
}
