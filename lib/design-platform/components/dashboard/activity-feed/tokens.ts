import { v } from "@/lib/design-platform/components/core";

/** ActivityFeed component tokens — references Phase 1 TBDP variables only. */
export const A_C_T_I_V_I_T_Y_F_E_E_D_TOKENS = {
  color: v.color.text.primary,
  background: v.color.surface.base,
  border: v.color.border.default,
  radius: v.radius.md,
  padding: v.spacing.md,
  gap: v.spacing.sm,
  shadow: v.shadow["1"],
  font: v.font.body,
} as const;
