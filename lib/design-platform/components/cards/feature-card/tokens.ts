import { v } from "@/lib/design-platform/components/core";

/** FeatureCard component tokens — references Phase 1 TBDP variables only. */
export const F_E_A_T_U_R_E_C_A_R_D_TOKENS = {
  color: v.color.text.primary,
  background: v.color.surface.base,
  border: v.color.border.default,
  radius: v.radius.md,
  padding: v.spacing.md,
  gap: v.spacing.sm,
  shadow: v.shadow["1"],
  font: v.font.body,
} as const;
