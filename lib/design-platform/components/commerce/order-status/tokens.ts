import { v } from "@/lib/design-platform/components/core";

/** OrderStatus component tokens — references Phase 1 TBDP variables only. */
export const O_R_D_E_R_S_T_A_T_U_S_TOKENS = {
  color: v.color.text.primary,
  background: v.color.surface.base,
  border: v.color.border.default,
  radius: v.radius.md,
  padding: v.spacing.md,
  gap: v.spacing.sm,
  shadow: v.shadow["1"],
  font: v.font.body,
} as const;
