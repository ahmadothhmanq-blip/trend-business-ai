import { v } from "@/lib/design-platform/components/core";

/** ProductDetails component tokens — references Phase 1 TBDP variables only. */
export const P_R_O_D_U_C_T_D_E_T_A_I_L_S_TOKENS = {
  color: v.color.text.primary,
  background: v.color.surface.base,
  border: v.color.border.default,
  radius: v.radius.md,
  padding: v.spacing.md,
  gap: v.spacing.sm,
  shadow: v.shadow["1"],
  font: v.font.body,
} as const;
