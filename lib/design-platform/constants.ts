/** Trend Business AI Design Platform (TBDP) — package identity. */
export const TBDP_PACKAGE_ID = "trend-business-ai-design-platform" as const;

/** Semantic version of the design platform foundation spec. */
export const TBDP_SPEC_VERSION = "1.0.0" as const;

/** Phase 1 scope identifier — foundations. */
export const TBDP_PHASE = "foundations-1" as const;

/** Phase 2 scope identifier — enterprise UI components. */
export const TBDP_UI_PHASE = "components-2" as const;

/** UI component system version. */
export const TBDP_UI_VERSION = "2.0.0" as const;

/** Phase 3 scope identifier — experience & interaction system. */
export const TBDP_EXPERIENCE_PHASE = "experience-3" as const;

/** Experience system version. */
export const TBDP_EXPERIENCE_VERSION = "3.0.0" as const;

/** Experience layer data-attribute prefix. */
export const TBDP_EXPERIENCE_PREFIX = "tbdp-xp" as const;

/** Phase 4 scope identifier — sector design DNA system. */
export const TBDP_SECTOR_DNA_PHASE = "sector-dna-4" as const;

/** Sector DNA system version. */
export const TBDP_SECTOR_DNA_VERSION = "4.0.0" as const;

/** Phase 5 scope identifier — integration layer. */
export const TBDP_INTEGRATION_PHASE = "integration-5" as const;

/** Integration layer version. */
export const TBDP_INTEGRATION_VERSION = "5.0.0" as const;

/** CSS custom property prefix for TBDP tokens. */
export const TBDP_CSS_VAR_PREFIX = "tbdp" as const;
