import type { TbdpOpacityTokens } from "@/lib/design-platform/foundations/color/types";

/** Opacity scale — referenced by semantic overlay and state tokens. */
export const TBDP_OPACITY_TOKENS: TbdpOpacityTokens = {
  transparent: 0,
  subtle: 0.04,
  muted: 0.08,
  soft: 0.12,
  medium: 0.24,
  strong: 0.48,
  opaque: 1,
};
