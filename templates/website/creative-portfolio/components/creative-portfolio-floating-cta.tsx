"use client";

/** Hidden — Kinetic DNA uses filmstrip + colophon, not floating CTA chrome. */
export function CreativePortfolioFloatingCta(_props: {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
}) {
  return null;
}
