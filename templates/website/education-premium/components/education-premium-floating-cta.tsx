"use client";

/** Floating CTA disabled for editorial magazine DNA — keep export for registry compatibility. */
type EducationPremiumFloatingCtaProps = {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
};

export function EducationPremiumFloatingCta(_props: EducationPremiumFloatingCtaProps) {
  return null;
}
