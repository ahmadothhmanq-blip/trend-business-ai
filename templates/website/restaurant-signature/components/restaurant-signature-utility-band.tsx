"use client";

/** Hidden on Forest home — menu document replaces utility CTA bands. */
export function RestaurantSignatureUtilityBand(_props: {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
}) {
  return null;
}
