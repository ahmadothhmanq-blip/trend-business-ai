"use client";

/** Hidden — Forest DNA is a tasting-menu document, not floating CTA chrome. */
export function RestaurantSignatureFloatingCta(_props: {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
}) {
  return null;
}
