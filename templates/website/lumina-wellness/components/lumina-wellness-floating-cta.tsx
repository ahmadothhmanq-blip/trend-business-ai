"use client";

/** Floating CTA disabled for Lumina DNA — atmosphere pages stay quiet. */
type LuminaWellnessFloatingCtaProps = {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
};

export function LuminaWellnessFloatingCta(_props: LuminaWellnessFloatingCtaProps) {
  return null;
}
