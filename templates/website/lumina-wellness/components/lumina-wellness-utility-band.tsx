"use client";

/** Utility band unused on home — Lumina DNA is sparse atmosphere, not CTA theatre. */
type LuminaWellnessUtilityBandProps = {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
};

export function LuminaWellnessUtilityBand(_props: LuminaWellnessUtilityBandProps) {
  return null;
}
