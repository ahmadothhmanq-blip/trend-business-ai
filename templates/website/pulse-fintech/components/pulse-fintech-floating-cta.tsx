"use client";

/** Floating CTA stripped for live ticker / stream DNA. */
type PulseFintechFloatingCtaProps = {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
};

export function PulseFintechFloatingCta(_props: PulseFintechFloatingCtaProps) {
  return null;
}
