"use client";

/** Utility band omitted from stream home flow. */
type PulseFintechUtilityBandProps = {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
};

export function PulseFintechUtilityBand(_props: PulseFintechUtilityBandProps) {
  return null;
}
