"use client";

/** Utility band omitted from home mosaic flow — export retained for registry. */
type PrismAuroraUtilityBandProps = {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
};

export function PrismAuroraUtilityBand(_props: PrismAuroraUtilityBandProps) {
  return null;
}
