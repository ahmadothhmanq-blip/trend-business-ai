"use client";

/** Floating CTA stripped for asymmetric bento DNA — keep export for registry. */
type PrismAuroraFloatingCtaProps = {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
};

export function PrismAuroraFloatingCta(_props: PrismAuroraFloatingCtaProps) {
  return null;
}
