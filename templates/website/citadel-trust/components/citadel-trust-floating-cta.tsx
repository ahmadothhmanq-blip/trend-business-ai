"use client";

/** Floating CTA disabled for Citadel DNA — trust stack has no floating marketing chrome. */
type CitadelTrustFloatingCtaProps = {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
};

export function CitadelTrustFloatingCta(_props: CitadelTrustFloatingCtaProps) {
  return null;
}
