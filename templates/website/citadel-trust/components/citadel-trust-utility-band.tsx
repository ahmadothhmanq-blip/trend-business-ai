"use client";

/** Utility band unused on home — Citadel DNA is a stacked dossier, not CTA theatre. */
type CitadelTrustUtilityBandProps = {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
};

export function CitadelTrustUtilityBand(_props: CitadelTrustUtilityBandProps) {
  return null;
}
