"use client";

/** Hidden on Estates home — dossier flow replaces utility CTA bands. */
export function RealEstatePremiumUtilityBand(_props: {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
}) {
  return null;
}
