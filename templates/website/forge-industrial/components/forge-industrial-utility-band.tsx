"use client";

/** Utility band unused on home — Forge DNA uses title-block nav instead of CTA theatre. */
type ForgeIndustrialUtilityBandProps = {
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  primaryCta?: string;
  secondaryCta?: string;
};

export function ForgeIndustrialUtilityBand(_props: ForgeIndustrialUtilityBandProps) {
  return null;
}
