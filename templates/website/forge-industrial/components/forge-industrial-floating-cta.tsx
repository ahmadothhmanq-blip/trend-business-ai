"use client";

/** Floating CTA disabled for Forge DNA — schematic pages have no floating marketing chrome. */
type ForgeIndustrialFloatingCtaProps = {
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
};

export function ForgeIndustrialFloatingCta(_props: ForgeIndustrialFloatingCtaProps) {
  return null;
}
